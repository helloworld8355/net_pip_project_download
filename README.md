# net_pip_project_download
Python 3.6 或以上（Python 3.7+ 更佳）

操作系统：Windows / macOS / Linux 均可

网络：本机测试无需特殊配置，局域网使用需确保服务器端口（默认 12345）未被防火墙阻止

两个文件均只用到了 socket、threading、os、re 等标准库模块，无需 pip install，直接使用系统 Python 即可。
